resource "aws_dynamodb_table" "aggregation" {
  name         = "alarm-aggregation"
  billing_mode = "PAY_PER_REQUEST"
  hash_key     = "alarm_key"

  attribute {
    name = "alarm_key"
    type = "S"
  }

  ttl {
    attribute_name = "expires_at"
    enabled        = true
  }
}
